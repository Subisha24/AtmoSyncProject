services:
  superset:
    build: .
    container_name: atmosync_superset

    ports:
      - "8088:8088"

    environment:
      SUPERSET_SECRET_KEY: atmosync_super_secret_key

    volumes:
      - superset_home:/app/superset_home

volumes:
  superset_home: